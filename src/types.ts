export interface NavItem {
    icon?: string;
    label: string;
    active?: boolean;
  }
  
  export interface Playlist {
    id: string;
    title: string;
    description: string;
    author: string;
  }